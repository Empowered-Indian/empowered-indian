import ReactEChartsCore from 'echarts-for-react/lib/core'
import echarts from '../../../../utils/echartsConfig'

const OptimizedEChart = (props: any) => <ReactEChartsCore echarts={echarts} {...props} />

export default OptimizedEChart

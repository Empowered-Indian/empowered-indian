# Use pnpm for this workspace package
# This tells build systems to use pnpm instead of npm
engine-strict=true
package-manager-strict=false

# Inherit from root pnpm config
shamefully-hoist=true
auto-install-peers=true
